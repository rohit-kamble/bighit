import { Text } from 'react-native';
import styled from 'styled-components';

export const Container = styled(Text)({
    color: 'white',
    fontFamily: 'futura',
    fontSize: 24,
    textAlign: 'center'
});
